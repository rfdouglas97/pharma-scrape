# Wayback BMS POC (reference)

Proof-of-concept for the 5-year longitudinal backfill (see ../../WAYBACK_BACKFILL_PLAN.md).
Throwaway scripts + the resulting silver (extractions/) + reports. Large renders (22MB PNG) excluded;
re-fetch from Wayback via render_all.py. Read MORNING_SUMMARY.md first.

The SCALABLE version of this lives in pipeline_intel/ (DB-backed change detection), not here.
